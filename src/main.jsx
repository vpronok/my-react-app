import React from 'react';
import ReactDOM from 'react-dom/client';
import AIWordPressBuilderApp from './components/AIWordPressBuilderApp';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <AIWordPressBuilderApp />
  </React.StrictMode>,
);
